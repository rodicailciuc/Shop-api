<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Bags</title>
</head>
<body>
    <%- include('./includes/nav.ejs', {token : token}) %>
    <header>
        <h1>Products</h1>
        <% if(email) {%> 
            <h3>Hello, <%= email %></h3>
        <% }; %>
        <hr/> 
        <% products.forEach((product) => { %>
            <div class="product">
                <divdiv class="img" >
                    <img src="<%= product.img %>" alt=" <%= product.name %>">
                </div>

                <h2><%= product.name %></h2>
                <p><%= product.price %>€</p>
                <p><%= product.description %></p>

                <button>
                    <a href="api/products/<%= product.id %>"></a>
                </button>
            </div>
            
       <%  }); %>
    </header>
</body>
</html>